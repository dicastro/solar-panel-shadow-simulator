* He actualizado en tu contexto el código con la última versión. Esta versión no es una versión productiva todavía, por lo que se puede hacer cualquier refactor sin miramientos, ya que no va a haber afectación a usuarios.

* Lo que enumero a continuación es un punto clave de la aplicación, así que cuando proceses mis comentarios analiza en profundidad el código actual para identificar los comportamientos que describo. Lo que trato de describir es lo que quiero que haga la aplicación. El código que hay ahora no funciona correctamente ni se comporta como quiero.

* He afinado la configuración del `config.json` para hacer una simulación lo más próxima a la realidad. Y me he dado cuenta de varios fallos que son importantes y que hay que afrontar lo antes posible antes de continuar con la inclusión de nuevas funcionalidades. Ya que lo prioritario es generar una simulación lo más fiel/próxima posible a la realidad. Como ya tengo una instalación que lleva funcionando durante 3 años, tengo datos reales que utilizo como baseline para ver si la simulación se aproxima o se aleja. En el `config.json` hay un setup con label _configuracion actual_ que representa mi instalación actual. A continuación enumero los fallos que he visto

a) El azimuth se puede configurar para el site y para los paneles. Esto es correcto. Ambos son azimuths absolutos, no depende uno del otro. Así es más fácil para el usuario que medirá el azimuth de cada cosa, en lugar de uno relativo al otro. Esto es correcto. La cosa es que los valores están invertidos. 0 es el valor de azimuth para el Sur. Para el azimuth del site un valor positivo va hacia el Este. Sin embargo, un valor positivo para el azimuth de los paneles va hacia el Oeste. En el `README.md` en un momento dice esto `- Positive values rotate towards West; negative towards East.`. No sé si hay más sitios en el `README.md` donde se mencione/explique el azimuth. Quiero que el código se comporte igual para el azimuth de paneles y para el del site. Quiero hacer que hacia el Este sea positivo y que hacia el Oeste sea negativo. Corrige el código y el `README.md`

b) Los paneles no se están posicionando bien. Cuando tenía en la configuración azimuth 0, aparentemente lo veía todo bien en el renderizado. Pero al poner los valores reales de azimuth, y además valores que son diferentes para el site y para los arrays de paneles (en algún setup), se ha hecho evidente que los arrays de paneles no se están renderizando bien. Como referencia (o punto de partida) debería tomarse el Suroeste. A la hora de situar un panel debería usarse la esquina inferior-izquierda (visto desde arriba, con el Norte arriba y Sur abajo) como punto de referencia. La posición configurada para el array debería situar la esquina inferior-izquierda del primer panel con respecto a la esquina inferior-izquierda (visto desde arriba, con el Norte arriba y Sur abajo) de la terraza. Cuando la `x` de la posición del array aumenta, nos vamos hacia el Este y cuando la `z` de la posición del array aumenta nos vamos hacia el norte. Al posicionar un array de paneles, los paneles están alineados entre sí como si formasen un único panel más grande (teniendo en cuenta que hay una separación configurada entre los paneles del array). Si un panel es un rectángulo, el resultado del array es otro rectánculo más grande. Ahora mismo no se situan así, y forman como una escalera (diría que tiene que ver por cómo se está renderizando en Three.js y por las rotaciones o giros que se aplican). Además parece que se toma la esquina inferior-derecha (visto desde arriba, con el Norte arriba y Sur abajo) del panel como punto de partida y se situa en la instalación (suelo de la terraza) yendo hacia el Oeste (izquierda, visto desde arriba). Además los sucesivos paneles del array se ubican hacia el Oeste (izquierda, visto desde arriba), en lugar de ir hacia el este (derecha, visto desde arriba). Cuando el array tiene varias filas, la fila 0 será la de más al sur y las sucesivas filas irán hacia el norte. Con respecto a las columnas, la columna 0 será la del Oeste y las sucesivas columnas irán hacia el Este. La coordenada (0, 0) dentro del array corresponde con el Suroeste del array. Algo relacionado con esto último está descrito más adelante.

c) Creo que al aplicar un azimuth distinto a 0 a los paneles, la inclinación resultante que tienen no es solo en un eje, sino que es en varios, y creo que tiene que ver con la rotación que implica el azimuth. Si la configuración dice que el array tiene X grados de inclinación, esta inclinación es con respecto al suelo y los paneles tienen que ser paralelos al suelo y tener esa inclinación con respecto al suelo. Tanto el lado del panel que está más cercano al suelo como el que está más alto tienen que quedar paralelos al suelo. Esto no está pasando.

d) Como he mencionaod antes (y creo que esto ya se está intentando hacer así), todos los paneles de un array forman un bloque, un rectánculo más grande. La altura con respecto al suelo será para la parte más baja de los paneles de la primera fila del array, el resto de filas del array tendrán que situarse a continuación de la parte más alta de la fila anterior, de tal forma que todo el conjunto de paneles del array tenga la inclinación con respecto al suelo configurada en `config.json`

e) Todos estos cambios para corregir el posicionamiento de los paneles tienen que tener en cuenta la disposición de los puntos de muestreo para el raycasting que se utiliza tanto para el cálculo de consumo instantáneo en el renderizado como para el cálculo de la simulación anual.

f) Otra cosa con respecto a la disposición de los paneles. Aprovechando que hay que darle una vuelta a esto para que el azimuth funcione correctamente, describo otra cosa que he observado y que me gustaría corregir aprovechando la coyuntura. Hay una discrepancia entre la forma de hacer override de la configuración de los paneles (en `arraysSettings` dentro de cada setup) y la forma de hacer override de la configuración de los muros (en `wallSettings`). Para `wallSettings` el muro `0` es el del Suroeste, y se sigue en el sentido contrario a las agujas del reloj. Esto es un comportamiento previo al del `arraysSettings`. Para `arraysSettings` la `row: 0` debería ser la del Sur y la `col: 0` la del Oeste, sin embargo, la `row: 0` se refiere a la del Norte. En `arraysSettings` se empieza en el Noroeste y se va hacia el Este y el Sur a medida que incrementa `col` y `row` respectivamente. Quiero que en `arraysSettings` la posición `row: 0, col: 0` se refiera al Suroeste y se vaya hacia el Este y el Norte a medida que incrementa `col` y `row` respectivamente. Se ha hecho un intento de cambiar esto cambiando el cálculo de `localZ` en `SolarPanelFactory`. Este cambio ha sido infructuoso porque afectaba al cálculo de la simulación anual, porque creo que entonces al cambiar la posición de los paneles cambiaría la de los puntos de muestreo. Así que la solución, si implica forzosamente cambiar `localZ` en `SolarPanelFactory`, debería también hacer los cambios pertienentes en todos los puntos de muestreo utilizados en la simulación anual del worker. El cálculo del consumo instantáneo del instante renderizado parece que no se ve afectado por esto y funciona correctamente. Al cambiar esto, se tienen que actualizar los heatmap de los paneles en la sección de resultados para que se visualicen de la misma forma, empezando los arrays de abajo e izquierda y hacia la derecha y arriba.