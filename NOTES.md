* Visualizar los Strings de paneles en el renderizado
  * cada string deberia tener un color asignado automáticamente
  * la visualización del string se implementaria poniendo los bordes del panel (los cantos) con el color del string
  * esto deberia ser coherente en toda la aplicación y deberia mostrarse en el renderizado, en el sidebar de resultados de simulaciones al mostrar los heatmaps de los arrays de los paneles y al generar un pdf con los resultados de simulaciones
* La caché de las simulaciones no se está limpiando cuando toca. Diría que ahora mismo únicamente se vuelve a ejecutar el cálculo cuando cambia alguno de los valores de la sección "Simulación anual" (irradiancia, intervalo, año, puntos por zona, umbral sombra zona). Pero esto no es suficiente, la caché se tiene que "invalidar" cuando cambia cualquier valor que pueda afectar al resultado de la simulación: coordenadas del site, configuración de muros, suelo, arrays... vamos creo que sería más fácil decir qué configuraciones/parámetros se podrían tocar sin invalidar la cache que la inversa. De hecho ahora mismo no se me ocurre ningún parámetro/configuración que se pueda cambiar y que no afecte al cálculo de la simulación anual. Si no estoy equivocado, la excepción sería el "timezone", creo que esto no afectaría para nada a la simulación y un cambio en el timezone no debería invalidar la cache, cualquier otro cambio debería invalidarlo. Así que no sé si como propuesta se podría utilizar un hash de la configuración combinado con los parámetros de la UI para determinar si se invalida la cache o no. La clave de la caché creo que podría ser la misma, para así saber si hay que reemplazar el valor cacheado o si hay que incluir un nuevo valor a la caché. Aunque esto no sé muy bien cómo hacerlo, no me gustaría que la caché creciese mucho por ir variando un poco la configuración. Te pediría que hicieras un análisis y me propusieras varias alternativas, porque no es tan trivial como parece. Creo que habría que prevalecer la facilidad y claridad del usuario frente a optimizar el recálculo. Si hay una alternativa que implica recalcular más veces pero para el usuario es más claro y sencillo, prefiero esa opción, antes que otra solución donde se recalcule únicamente cuando es estrictamente necesario, pero que para el usuario sea inmanejable la gestión de la caché.
* Incluir una ayuda sobre la simulación anual orientada al usuario final. Tiene que explicar en qué consiste la simulación geométrica y la de open-meteo. Por qué open-meteo es únicamente para el pasado y la geométrica permite el año en curso. Por qué la geométrica permite intervalos de tiempo más pequeños. En qué consiste los "puntos NxN por zona", con alguna imagen que los represente con varios ejemplos (con svg embebido). También en qué consiste el "Umbral sombra zona". Las implicaciones del total puntos de muestreo, pasos de tiempo y total rayos. Cómo funcionan los workers (esto a nivel funcional, sin entrar en tecnicismos, por ejemplo la palabra worker ya sería un tecnicismo, habría que decir que es un cálculo en paralelo y que la paralelización depende del procesador del ordenador). Indicar que estas simulaciones se quedan guardadas en la caché. Que en el sidebar de configuración se puede ver esta cache y gestionarla. La guía sería en formato html, las imágenes explicativas con svg embebidos. Se puede tomar como referencia la guia de usuario para la configuración. Esta guía también tiene que ser multiidioma, e inicialmente en los 2 idiomas soportados: inglés y español. En la sección de "Simulación anual" de la UI incluir un enlace a esta guía con un pequeño mensaje indicando que está disponible.
* Alguna vez he detectado que el cambio de idioma tardaba demasiado y durante el cambio parecía que no estaba ocurriendo nada. No he conseguido detectar cuando pasa, no pasq siempre. No sé si es la primera vez que se cambia de idioma tras arrancar o cuando. No sé si se estaria triggeando algún cálculo complejo que hace que el cambio de idioma por primera vez tarde tanto. Habria que analizar si el cambio de idioma podria estar triggeando por error algún calculo complejo.
